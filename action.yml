name: 'Shukka Publish'
description: 'Publish an electron-builder or Tauri output directory to a Shukka update channel'
author: 'shukka-app'
branding:
  icon: 'upload-cloud'
  color: 'blue'

inputs:
  server-url:
    description: 'Base URL of the Shukka instance, e.g. https://updates.example.com'
    required: true
  api-key:
    description: 'Shukka API key bound to the target app'
    required: true
  app:
    description: 'App slug as configured in Shukka'
    required: true
  channel:
    description: 'Target channel'
    required: false
    default: 'stable'
  directory:
    description: 'electron-builder or Tauri output directory containing artifacts and latest*.yml or latest.json'
    required: false
    default: 'dist'
  version:
    description: 'Release version; read from latest*.yml or latest.json when omitted'
    required: false
    default: ''
  create-channel:
    description: 'Create the channel if it does not exist yet'
    required: false
    default: 'false'
  release:
    description: 'If true, the version goes live and the feed can see it. Default false leaves a draft the feed cannot see; promote later in the panel or PATCH the channel currentVersion.'
    required: false
    default: 'false'

outputs:
  version:
    description: 'The published version'
  channel:
    description: 'The channel the version was published to'

# JavaScript action: the runner's Node executes the uploader directly.
# No bash/pwsh/cmd, so Windows self-hosted runners with only MinGit work.
runs:
  using: 'node24'
  main: 'scripts/shukka-upload.mjs'
